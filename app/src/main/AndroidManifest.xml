<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.warunakavinda.parkingmate">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
    android:name="com.globalvariable.globalclass"
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application


        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="ParkingMate"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".start"
            android:label="ParkingMate"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".testp"
            android:label="@string/title_activity_testp"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AcmainActivity"
            android:label="@string/title_activity_acmain"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".availableplaces"
            android:label="@string/title_activity_availableplaces"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".sliit"
            android:label="Test Park 2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".other"
            android:label="@string/title_activity_other"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".inquries"
            android:label="@string/title_activity_inquries"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".register"
            android:label="@string/title_activity_register"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".about"
            android:label="@string/title_activity_about"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             ATTENTION: This was auto-generated to add Google Play services to your project for
             App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />



        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".reroute"
            android:label="@string/title_activity_reroute" />
        <activity
            android:name=".start_jrny"
            android:label="Rerouting"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>
